export const API_URL: string = 'htrgt';

class SubsonicClient {
    private username: string;
    private password: string;

    constructor(username: string, password: string) {
        this.username = username;
        this.password = password;
    }

    scrapSongs() {
        
    }
}